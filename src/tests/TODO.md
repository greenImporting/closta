sqlite.py: crud operations, db init, error handling.

main.py: task building, loading, new/edit/delete callbacks (mock dpg and db).

tray.py: thread spawning, window lock, graceful exit (mock pystray and window).