[project]
name = "closta"
version = "0.4"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "dearpygui",
    "pywin32",
]

[dependency-groups]
test = [
    "pytest>=8.3.5",
]

[project.optional-dependencies]
dev = [
    "nuitka",
]