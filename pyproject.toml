[project]
name = "closta"
version = "0.1a3"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "dearpygui",
    "pywinctl",
    "pystray"
]

[dependency-groups]
test = [
    "pytest>=8.3.5",
]
