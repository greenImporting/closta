[project]
name = "closta"
version = "0.2"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "dearpygui",
    "pywinctl",
    "pystray",
    "pymonctl",
    "pillow",
    "pyautogui",
]

[dependency-groups]
test = [
    "pytest>=8.3.5",
]